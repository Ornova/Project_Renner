package upnow;

/**
 * @author Julian Helfenstein
 * @author Christopher Hilfing
 * @author Christopf Bodschwinna
 * @author Daniel Blättler
 * @since 18.01.2018
 * @version 0.1
 * @depreceated false
 */
public class VerketteteListe {

    /**
     *
     * @param args
     */
    /*
    public static void main(String[] args) {

        LinkedList neueListe = new LinkedList();

        // Test
        /*
        // Test-Variables
        int in = 6;
        String an = "test";

        // Test-Constructors
        neueListe.insert(in, in, an, an);
        neueListe.insert(in, in, an, an);
        neueListe.insert(in, in, an, an);
        neueListe.insert(in, in, an, an, an);
        neueListe.insert(in, in, an, null);
        neueListe.init();
         
        //Ausgabe der Linkliste
    
        * /
    
        System.out.print(neueListe.getErsterKnoten().getData() + "\t");
        System.out.print(neueListe.getErsterKnoten().getData2() + "\t");
        System.out.print(neueListe.getErsterKnoten().getData3() + "\t");
        if (neueListe.getErsterKnoten().getData4() != null) {               // Nicht immer existierend
            System.out.print(neueListe.getErsterKnoten().getData4() + "\n");
        }
        while (neueListe.next() != null) {
            System.out.print(neueListe.getAktuellerKnoten().getData() + "\t");
            System.out.print(neueListe.getAktuellerKnoten().getData2() + "\t");
            System.out.print(neueListe.getAktuellerKnoten().getData3() + "\t");
            if (neueListe.getAktuellerKnoten().getData4() != null) {
                System.out.print(neueListe.getAktuellerKnoten().getData4() + "\n");
            }
        }
    }
     */
}
